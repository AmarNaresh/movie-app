import React from 'react';

const SearchMovieList = (props) => {
    const {search}= props;
    console.log(search);
    return (
        <div>

        </div>
    );
};

export default SearchMovieList;